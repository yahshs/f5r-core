# إصلاح البوت والكمية — تحديث وصف الفاتورة 2026-09-12

## ما تغير

- بناءً على شكل `invoice.created` المرسل، تُقرأ الكمية المتغيرة من وصف عنصر الفاتورة: `عدد المشاهدات : 1000.`، حتى لو بقي اسم الحقل في القاعدة `اختر عدد`. العدد `quantity: 1` هو عدد مرات شراء الخدمة ويُستخدم مضاعفًا للعدد المختار. الكمية الثابتة تؤخذ من إعداد قاعدة المنتج؛ لا يوجد عدد الخدمة الثابت في الفاتورة المرفقة.
- يُستخرج رابط كل عنصر دون نقطة نهاية الجملة أو أقواس Markdown. ستة أسطر لنفس المنتج وبروابط مختلفة تنتج ستة تنفيذات مستقلة، 1000 لكل رابط في المثال؛ ولا تُدمج إلى رابط واحد بكمية 6000.
- تبقى أوصاف الفاتورة محفوظة عند جلب تفاصيل سلة. يُطابق `item_id` في الفاتورة مع `id` لعنصر الطلب، وتُحفظ هوية السجلات القديمة عند إعادة الاستقبال كي لا تتكرر عمليات التنفيذ.
- الترحيل الجديد 039 يعيد محاولة أخطاء `Quantity value missing` غير المرسلة، حتى إذا كان الترحيل 037 قد نُفذ سابقًا. يستبعد الطلبات ذات رقم مزود أو كمية مرسلة أو محاولة بديلة؛ لا يعيد إرسال طلب ناجح.
- عند غياب حقل العدد، يستعيد العامل بيانات عنصر الطلب الأصلي ثم يجلب تفاصيل الطلب وخيارات عناصره من سلة، إذا توفر اتصال API صالح. يحفظها في نفس السجل، ولا ينشئ طلبًا جديدًا لإصلاح البيانات.
- يحتفظ بمعرف سلة الداخلي لاستدعاءات API، والرقم المرجعي للعرض. يطابق العناصر بمعرف العنصر أو منتج/SKU فريد، وليس بترتيبها.
- لا يستخدم الأسعار أو معرفات الخيارات بدل الكمية، ويرفض الاختيارات المتعارضة. ربط الحقل بـ `quantity` لا يضاعف الكمية في نفسها.
- الترحيل 037 يعيد محاولة الأخطاء القديمة المحددة بالكمية فقط، مع استبعاد المحاولات المرسلة للمزود أو التي لها محاولة بديلة. يعيد العامل المحاولة ثلاث مرات إضافية عند استمرار غياب البيانات، ثم يترك سببًا واضحًا للمراجعة.
- فشل إرسال رد تيليجرام يرجع 503 لإتاحة إعادة التسليم. إيصالات التحديث تمنع إعادة معالجة تحديث سبق نجاحه، مع مهلة استرداد إذا توقف العامل.
- حفظ إعدادات البوت يتحقق من التوكن عبر `getMe` ويسجل الويب هوك ويستخرج اسم البوت الصحيح. فشل الربط ظاهر في الواجهة، ولا يتم إسقاط الرسائل المعلقة. لا يُسجل التوكن الجديد في سجل التدقيق.
- أضيف فحص الربط وإصلاحه في إعدادات الأدمن. رابط خادم البوت مستقل عن إعدادات سلة. عنوان Railway العام له الأولوية على عنوان واجهة قديم؛ ويمكن تحديد عنوان الخادم صراحةً.
- لم يتغير رابط ويب هوك سلة أو شرط قبول `invoice.created` أو منطق الدفع والتعويض لدى المزود.

## بعد رفع النسخة ونشرها على Railway

1. افتح لوحة الإدارة ← الإعدادات ← Telegram.
2. Bot Token هو رمز API من BotFather، وليس اسم البوت. احفظه في المنصة فقط؛ لا تضعه في GitHub أو ترسله بالمحادثة.
3. اضغط «إصلاح الربط» ثم «فحص ربط البوت». يعرض الفحص اسم البوت الفعلي والرابط المسجل والرسائل المعلقة وآخر خطأ تسليم.
4. إذا لم يُكتشف عنوان Railway، احفظ رابط خدمة المنصة العام `https://…up.railway.app` في «رابط خادم البوت». ليس رابط `t.me` ولا رابط متجر سلة.
5. أرسل `/start` ثم رقم طلب موجود. لا تختبر التعويض على طلب حقيقي لمجرد فحص الاتصال.

للكمية المتغيرة، يكفي وصف الفاتورة الذي يتضمن اسم حقل العدد وقيمته؛ لا يحتاج المثال المرفق إلى توكن API لسلة. إذا خلا الوصف والخيارات معًا من العدد، يلزم توفير بياناته أو ربط سلة بصلاحية قراءة الطلبات. لن يخمن النظام العدد أو يرسل كمية 1. للكمية الثابتة، يجب ضبط العدد في قاعدة المنتج.

## التحقق وحدوده

- نجحت 78 حالة في `npm run test:regression` بتاريخ 12 سبتمبر: استقبال شكل الفاتورة المرفق عبر HTTP وتنفيذ أسطرها الستة بمحاكاة المزود، العدد من الوصف بالعربي وصيغ K/M، المحافظة على اقتران الرابط بكميته عند تغيير ترتيب عناصر API، الكمية الثابتة، إعادة المحاولة والترحيل 039 دون تكرار، اختبارات البوت والدخول السابقة. البيانات في الاختبارات مصطنعة، ولم تُنسخ معلومات العملاء أو عناوينهم إلى المشروع.
- نجح `npm run build`.
- في فحص 9 سبتمبر السابق، شُغّل الفحص الشامل وقورن بالمصدر الأصلي `acb8934769381b6aa6ad1b591d7eb4300f4c8b42` تحت نفس تجهيز الاختبار المعزول. بقيت 31 حالة قديمة فاشلة في اختبارات سلة وإشعارات التاجر ترسل أحداثًا بلا `invoice.created`. لم نغيّر شرط الحدث لإجبار تلك الاختبارات على النجاح. فحص 12 سبتمبر المحدد أعلاه لا يعني أن كامل الاختبارات القديمة أصبحت ناجحة.
- فحص TypeScript الفعلي السابق للواجهة والخادم أظهر أخطاء مطابقة للنسخة الأصلية (أنواع المنتجات/التصنيفات واستيراد Express). أمر `tsc --noEmit` على الإعداد الجذري الفارغ لا يُعد فحصًا شاملًا، ولا ندعي اجتياز فحص الأنواع الكامل.
- تعذر سابقًا تشغيل `npm start` في بيئة الفحص بسبب منع قناة IPC الخاصة بـ tsx بخطأ EPERM قبل بدء الخادم. اختبارات HTTP تستخدم التطبيق وقاعدة بيانات اختبار معزولة، ولا تغني عن التأكد من نشر Railway.
- كل اتصالات Salla/Telegram/المزود في الاختبارات محاكاة. لا تستخدم أسرار الإنتاج ولا تطلب خدمة مدفوعة. لم تُختبر إعدادات حسابك الحية، ونجاح الاختبارات لا يثبت اكتمال نشر Railway أو وصول رسالة فعلية.
- لم تُرفع هذه التعديلات إلى GitHub؛ محاولة الرفع السابقة رُفضت لغياب صلاحية الكتابة. ملف ZIP هو تسليم النسخة الحالية.

مصدر شكل خيارات الطلب: https://docs.salla.dev/5565737e0

مصدر واجهة تيليجرام: https://core.telegram.org/bots/api
